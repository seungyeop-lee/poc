FROM node:20-alpine

WORKDIR /app

# package.json 복사 및 의존성 설치
COPY package.json ./
RUN npm install

# 소스 코드 복사
COPY scripts/ ./scripts/

# 포트 노출
EXPOSE 8443

# mTLS 서버 실행
CMD ["npx", "tsx", "scripts/mtls-server.ts"]
